"""
Admin para Clientes.

Nota: El modelo Client usa MongoDB (MongoEngine), no Django ORM.
Django Admin no es compatible con MongoEngine, por lo que este archivo
está vacío. Para administrar clientes, usa las vistas HTML o la API.
"""

# MongoDB no es compatible con Django Admin
# Los clientes se administran via /clients/ o /api/clients/